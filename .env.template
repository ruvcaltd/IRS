# ════════════════════════════════════════════════════════════════
# PRODUCTION ENVIRONMENT CONFIGURATION
# ════════════════════════════════════════════════════════════════
# IMPORTANT: Never commit actual credentials to this file
# Use this template as a guide only

# ─── SQL Server ───────────────────────────────
# CHANGE THIS: Generate strong password (min 8 chars with uppercase, lowercase, number, symbol)
# Example: openssl rand -base64 32
SA_PASSWORD=ChangeMeToStrongPassword123!
DB_NAME=IRS

# ─── JWT Authentication ──────────────────────
# CHANGE THIS: Min 32 chars, cryptographically random
# Generate with: openssl rand -base64 32
JWT_SECRET_KEY=ChangeMe_GenerateNewSecretKeyWith32CharactersMinimum1234
JWT_ISSUER=IRS.Api
JWT_AUDIENCE=IRS.Client

# ─── Third-Party API Keys ─────────────────────
# CHANGE THIS: Register new key at https://openfigidev.guidata.com/
# Old key has been ROTATED and invalidated
OPENFIGI_API_KEY=ChangeMe_GenerateNewKeyFromOpenFigi
LLM_ENCRYPTION_KEY=ChangeMe_GenerateNewWith_openssl_rand_base64_32
ENCRYPTION_KEY=ChangeMe_GenerateNewWith_openssl_rand_base64_32
ENCRYPTION_IV=ChangeMe_GenerateNewInitializationVector

# ─── API URLs ─────────────────────────────────
# For local development
DOTNET_API_BASE_URL=http://localhost:5000
FLASK_API_BASE_URL=http://localhost:5001

# For production (override in production deployment)
# DOTNET_API_BASE_URL=https://api.yourdomain.com
# FLASK_API_BASE_URL=https://api.yourdomain.com/finance
